# Test Strategy

This repository uses layered testing:

- Backend unit and integration tests inside `backend/`
- Frontend unit tests inside `frontend/`
- Optional cross-system scenarios under this root-level `tests/` folder

Use this folder for higher-level test plans and future cross-service automation.
